#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: the  int to be added to b
 * @b: the int to be added to a
 * Return: sum of b and a
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}

